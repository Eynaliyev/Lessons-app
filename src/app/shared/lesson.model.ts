export class Lesson {
  constructor(
    public id: number,
    public name: string,
    public studentCount: number,
    public language: string,
    public year: string,
    public semester: string
  ) {  }
}
